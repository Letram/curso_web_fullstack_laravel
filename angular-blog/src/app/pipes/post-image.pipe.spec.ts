import { PostImagePipe } from './post-image.pipe';

describe('PostImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PostImagePipe();
    expect(pipe).toBeTruthy();
  });
});
